use crate::math::Vec3D;
use crate::objects::{Object3D, Ray, Plane, Sphere, Triangle};

pub struct Scene {
    // TODO:
    // fields:
    // - container for *objects
    // - container for *light_sources
}

pub struct SceneRenderer {
    // TODO:
    // fields:
    // - FoV, z_near, z_far
    // - width, height
    // - camera object
    // methods: 
    // - get_color(Scene object, x, y)
    // - render_scene(Scene object)
}